import { Event } from '@/data/events'; import { getDictionary, localize, type Language } from '@/lib/i18n'; import { PriorityBadge } from './PriorityBadge';
export function EventCard({event,lang}:{event:Event;lang:Language}){const t=getDictionary(lang);return <article className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-slate-400">{event.day} · {event.time}</p><h3 className="mt-1 font-black text-white">{localize(event.title,lang)}</h3></div><PriorityBadge priority={event.importance} lang={lang}/></div><p className="mt-3 text-sm text-slate-300"><b>{t.type}:</b> {localize(event.type,lang)}</p><p className="mt-1 text-sm text-slate-300"><b>{t.preparation}:</b> {localize(event.preparation,lang)}</p></article>}
