import Link from 'next/link'; import { getDictionary, type Language } from '@/lib/i18n';
const nav=[['', 'navHome'],['orders','navOrders'],['strategy','navStrategy'],['calendar','navCalendar'],['roster','navRoster'],['guides','navGuides']] as const;
export function Navbar({lang}:{lang:Language}){const t=getDictionary(lang); const next=lang==='en'?'zh':'en'; return <aside className="hidden md:fixed md:inset-y-0 md:left-0 md:flex md:w-56 md:flex-col md:border-r md:border-white/10 md:bg-command-900/90 md:p-5"><Link href={`/${lang}`} className="text-2xl font-black text-neon">{t.appName}</Link><nav className="mt-8 grid gap-2">{nav.map(([href,key])=><Link className="rounded-xl px-3 py-2 text-sm font-bold text-slate-300 hover:bg-white/10 hover:text-white" key={key} href={`/${lang}/${href}`}>{t[key]}</Link>)}</nav><Link className="mt-auto rounded-xl border border-tactical/40 px-3 py-2 text-center text-sm font-bold text-tactical" href={`/${next}`}>{t.languageToggle}</Link></aside>}
