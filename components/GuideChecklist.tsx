import { Guide } from '@/data/guides'; import { localize, type Language } from '@/lib/i18n';
export function GuideChecklist({guide,lang}:{guide:Guide;lang:Language}){return <section className="rounded-2xl border border-white/10 bg-command-900/70 p-5"><h2 className="text-xl font-black text-white">{localize(guide.title,lang)}</h2><ul className="mt-4 space-y-3">{guide.items.map((item,i)=><li key={i} className="flex gap-3 text-sm text-slate-200"><span className="mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded border border-neon text-neon">✓</span>{localize(item,lang)}</li>)}</ul></section>}
