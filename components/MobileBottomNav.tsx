import Link from 'next/link'; import { getDictionary, type Language } from '@/lib/i18n';
const nav=[['','navHome'],['orders','navOrders'],['strategy','navStrategy'],['calendar','navCalendar'],['roster','navRoster'],['guides','navGuides']] as const;
export function MobileBottomNav({lang}:{lang:Language}){const t=getDictionary(lang); const next=lang==='en'?'zh':'en'; return <div className="fixed inset-x-0 bottom-0 z-50 border-t border-white/10 bg-command-950/95 p-2 backdrop-blur md:hidden"><nav className="grid grid-cols-7 gap-1 text-center text-[11px] font-bold"><Link href={`/${next}`} className="rounded-lg py-2 text-tactical">{lang.toUpperCase()}</Link>{nav.map(([href,key])=><Link key={key} href={`/${lang}/${href}`} className="rounded-lg py-2 text-slate-300 hover:bg-white/10">{t[key]}</Link>)}</nav></div>}
