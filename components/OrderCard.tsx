import { Order } from '@/data/orders'; import { getDictionary, localize, type Language } from '@/lib/i18n'; import { PriorityBadge } from './PriorityBadge';
export function OrderCard({order,lang}:{order:Order;lang:Language}){const t=getDictionary(lang); const statusLabel = { Open: t.statusOpen, 'In Progress': t.statusInProgress, Locked: t.statusLocked, Done: t.statusDone }[order.status]; return <article className="rounded-2xl border border-white/10 bg-command-900/80 p-5"><div className="flex items-center justify-between gap-3"><PriorityBadge priority={order.priority} lang={lang}/><span className="text-xs text-slate-400">{order.time}</span></div><h3 className="mt-4 text-xl font-black text-white">{localize(order.objective,lang)}</h3><p className="mt-2 text-sm text-slate-300">{localize(order.instructions,lang)}</p><dl className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-slate-500">{t.responsible}</dt><dd>{order.responsible}</dd></div><div><dt className="text-slate-500">{t.status}</dt><dd>{statusLabel}</dd></div></dl></article>}
