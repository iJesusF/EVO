import { PriorityBadge } from '@/components/PriorityBadge'; import { SectionHeader } from '@/components/SectionHeader'; import { strategySections } from '@/data/strategy'; import { getDictionary, getLanguageFromSearchParams, localize, type LocalizedSearchParams } from '@/lib/i18n';
export default async function Strategy({searchParams}:{searchParams?:Promise<LocalizedSearchParams>}){const lang=getLanguageFromSearchParams(await searchParams); const t=getDictionary(lang); return <><SectionHeader title={t.strategyTitle} subtitle={t.strategySubtitle}/><div className="grid gap-4 md:grid-cols-2">{strategySections.map(s=><section key={s.id} className="rounded-2xl border border-white/10 bg-command-900/70 p-5"><div className="flex items-center justify-between"><h2 className="text-xl font-black">{localize(s.title,lang)}</h2><PriorityBadge priority={s.priority} lang={lang}/></div><ul className="mt-4 space-y-2 text-sm text-slate-300">{s.bullets.map((b,i)=><li key={i}>▸ {localize(b,lang)}</li>)}</ul></section>)}</div></>}
