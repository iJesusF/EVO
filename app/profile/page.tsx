'use client';
import { useTranslation } from '@/components/I18nProvider';
import { roster } from '@/data/roster';
import { localize } from '@/lib/i18n';
const roles = ['R5','R4','Rally Lead','Defender','Scout','Support','Farmer'];
export default function Profile(){const {lang,t}=useTranslation(); const profile=roster[0]; return <section><h1 className="text-3xl font-black text-white">{t.profileTitle}</h1><p className="mt-2 text-sm text-slate-300">{t.profileSubtitle}</p><form className="mt-5 grid gap-4 rounded-2xl border border-white/10 bg-command-900/70 p-5 md:grid-cols-2">{[[t.player,profile.player_name],[t.totalPower,profile.total_power],[t.firstSquadPower,profile.first_squad_power],[t.vip,String(profile.vip_level)],[t.squadType,profile.main_squad_type],[t.heroes,profile.best_heroes],[t.timezone,profile.timezone],[t.availability,localize(profile.availability,lang)]].map(([label,value])=><label key={label} className="text-sm font-bold">{label}<input className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-3 py-2" defaultValue={value}/></label>)}<label className="text-sm font-bold">{t.role}<select className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-3 py-2" defaultValue={profile.alliance_role}>{roles.map(r=><option key={r}>{r}</option>)}</select></label><label className="text-sm font-bold md:col-span-2">{t.notes}<textarea className="mt-2 min-h-28 w-full rounded-xl border border-white/10 bg-black/30 px-3 py-2" defaultValue={localize(profile.notes,lang)}/></label><button className="rounded-xl bg-neon px-4 py-2 font-black text-black" type="button">{t.saveProfile}</button></form></section>}
