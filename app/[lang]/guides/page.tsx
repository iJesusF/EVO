import { GuideChecklist } from '@/components/GuideChecklist'; import { SectionHeader } from '@/components/SectionHeader'; import { guides } from '@/data/guides'; import { getDictionary, isLanguage, type Language } from '@/lib/i18n';
export default function Guides({params}:{params:{lang:string}}){const lang:Language=isLanguage(params.lang)?params.lang:'en'; const t=getDictionary(lang); return <><SectionHeader title={t.guidesTitle} subtitle={t.guidesSubtitle}/><div className="grid gap-4 md:grid-cols-2">{guides.map(g=><GuideChecklist key={g.id} guide={g} lang={lang}/>)}</div></>}
