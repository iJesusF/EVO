import { OrderCard } from '@/components/OrderCard'; import { SectionHeader } from '@/components/SectionHeader'; import { dontDoToday, orders } from '@/data/orders'; import { getDictionary, isLanguage, localize, type Language } from '@/lib/i18n';
export default function Orders({params}:{params:{lang:string}}){const lang:Language=isLanguage(params.lang)?params.lang:'en'; const t=getDictionary(lang); return <><SectionHeader title={t.ordersTitle} subtitle={t.ordersSubtitle}/><div className="grid gap-4 lg:grid-cols-2">{orders.map(o=><OrderCard key={o.id} order={o} lang={lang}/>)}</div><section className="mt-8 rounded-2xl border border-red-400/30 bg-red-500/10 p-5"><h2 className="text-xl font-black text-red-100">{t.noDoTitle}</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-slate-200">{dontDoToday.map((x,i)=><li key={i}>{localize(x,lang)}</li>)}</ul></section></>}
