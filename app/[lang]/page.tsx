import { alliance } from '@/data/alliance'; import { events } from '@/data/events'; import { orders } from '@/data/orders'; import { getDictionary, isLanguage, localize, type Language } from '@/lib/i18n'; import { SectionHeader } from '@/components/SectionHeader'; import { StatCard } from '@/components/StatCard'; import { EventCard } from '@/components/EventCard'; import { OrderCard } from '@/components/OrderCard';
export default function Home({params}:{params:{lang:string}}){const lang:Language=isLanguage(params.lang)?params.lang:'en'; const t=getDictionary(lang); return <><SectionHeader eyebrow={t.homeEyebrow} title={t.homeTitle} subtitle={t.homeSubtitle}/><div className="grid gap-4 md:grid-cols-3"><StatCard label={t.totalPower} value={alliance.totalPower} detail={alliance.season}/><StatCard label={t.vip18} value={String(alliance.vip18Count)} detail="Critical rally assets"/><StatCard label={t.seasonStatus} value="ACTIVE" detail={localize(alliance.status,lang)}/></div><section className="mt-6 rounded-2xl border border-neon/20 bg-neon/10 p-5"><p className="text-xs font-bold uppercase tracking-widest text-neon">{t.activeDirective}</p><p className="mt-2 text-xl font-black text-white">{localize(alliance.directive,lang)}</p></section><div className="mt-8 grid gap-6 lg:grid-cols-2"><section><h2 className="mb-4 text-2xl font-black">{t.nextEvents}</h2><div className="grid gap-3">{events.slice(0,3).map(e=><EventCard key={e.id} event={e} lang={lang}/>)}</div></section><section><h2 className="mb-4 text-2xl font-black">{t.quickOrders}</h2><div className="grid gap-3">{orders.slice(0,2).map(o=><OrderCard key={o.id} order={o} lang={lang}/>)}</div></section></div></>}
