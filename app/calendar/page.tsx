import { EventCard } from '@/components/EventCard'; import { SectionHeader } from '@/components/SectionHeader'; import { events } from '@/data/events'; import { getDictionary, getLanguageFromSearchParams, type LocalizedSearchParams } from '@/lib/i18n';
export default async function Calendar({searchParams}:{searchParams?:Promise<LocalizedSearchParams>}){const lang=getLanguageFromSearchParams(await searchParams); const t=getDictionary(lang); return <><SectionHeader title={t.calendarTitle} subtitle={t.calendarSubtitle}/><div className="grid gap-4">{events.map(e=><EventCard key={e.id} event={e} lang={lang}/>)}</div></>}
