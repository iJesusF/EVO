import { SectionHeader } from '@/components/SectionHeader';
import { getDictionary, getLanguageFromSearchParams, type LocalizedSearchParams } from '@/lib/i18n';
const columns=['player_name','total_power','first_squad_power','vip_level','main_squad_type','best_heroes','timezone','availability','alliance_role','is_admin','notes'];
export default async function ImportPage({searchParams}:{searchParams?:Promise<LocalizedSearchParams>}){const lang=getLanguageFromSearchParams(await searchParams); const t=getDictionary(lang); return <><SectionHeader title={t.importTitle} subtitle={t.importSubtitle}/><section className="rounded-2xl border border-white/10 bg-command-900/70 p-5"><h2 className="text-xl font-black">{t.downloadTemplate}</h2><code className="mt-3 block overflow-x-auto rounded-xl bg-black/40 p-3 text-sm text-neon">{columns.join(',')}</code><ol className="mt-5 list-decimal space-y-2 pl-5 text-slate-300"><li>Export Google Sheets roster as CSV.</li><li>Match columns to <code>csv/roster-import-template.csv</code>.</li><li>R4/R5 uploads CSV here after Supabase service-role server action is enabled.</li><li>Server action validates roles, VIP level, and ownership before inserting profiles.</li></ol></section></>}
