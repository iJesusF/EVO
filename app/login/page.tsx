import { SectionHeader } from '@/components/SectionHeader';
import { getDictionary, getLanguageFromSearchParams, type LocalizedSearchParams } from '@/lib/i18n';
import { isSupabaseConfigured } from '@/lib/supabase';
export default async function Login({searchParams}:{searchParams?:Promise<LocalizedSearchParams>}){const lang=getLanguageFromSearchParams(await searchParams); const t=getDictionary(lang); return <><SectionHeader title={t.loginTitle} subtitle={t.loginSubtitle}/><form className="max-w-xl rounded-2xl border border-white/10 bg-command-900/70 p-5"><label className="text-sm font-bold">{t.email}<input className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-3 py-2" type="email" placeholder="rally@33g.example"/></label><label className="mt-4 block text-sm font-bold">{t.password}<input className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-3 py-2" type="password" placeholder="••••••••"/></label><button className="mt-5 rounded-xl bg-neon px-4 py-2 font-black text-black" type="button">{t.signIn}</button><p className="mt-4 text-sm text-slate-300">{isSupabaseConfigured ? 'Supabase client is configured for auth integration.' : t.authNotice}</p></form></>}
