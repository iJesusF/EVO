import { GuideChecklist } from '@/components/GuideChecklist'; import { SectionHeader } from '@/components/SectionHeader'; import { guides } from '@/data/guides'; import { getDictionary, getLanguageFromSearchParams, type LocalizedSearchParams } from '@/lib/i18n';
export default async function Guides({searchParams}:{searchParams?:Promise<LocalizedSearchParams>}){const lang=getLanguageFromSearchParams(await searchParams); const t=getDictionary(lang); return <><SectionHeader title={t.guidesTitle} subtitle={t.guidesSubtitle}/><div className="grid gap-4 md:grid-cols-2">{guides.map(g=><GuideChecklist key={g.id} guide={g} lang={lang}/>)}</div></>}
